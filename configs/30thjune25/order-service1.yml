server:
  port: 8082

spring:
  application:
    name: order-service1

  datasource:
    url: jdbc:mysql://localhost:3306/microservices1
    username: root
    password: root

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
